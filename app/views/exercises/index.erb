<h1 style="font-family:impact;">Exercises recorded for today!</h1>
<div style="font-family:helvetica;">
Here are all of the exercise records for today:
<% @exercises.each do |exercise| %>
  <% if exercise.date == @today %>
    <ul>
      <li>Username: <% id=exercise.user_id %>
        <% current = User.find_by_id(id)%>
        <%= current.username %><br>
        Exercise: <%= exercise.name %><br>
        Duration <%= exercise.minutes %> minutes</li>
    </ul>
    <% end %>
<% end %>
</div>
